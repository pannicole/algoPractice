function calendarMatching(calendar1, dailyBounds1, calendar2, dailyBounds2, meetingDuration) {
  // Write your code here.
  let dailyLowerBound = Math.max(dailyBounds1[0], dailyBounds2[0])
  let dailyUpperbound = Math.max(dailyBounds1[1], dailyBounds2[1])
}
